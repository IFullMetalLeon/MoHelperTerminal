<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MoHelperTerminal.View.IODoc.IODocPage"
             Title="Прием товара">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding DocNumb}" HorizontalTextAlignment="Center"/>
            <Label Text="{Binding DocDate}" HorizontalTextAlignment="Center"/>
            <ListView x:Name="ioDocSpec"
                      ItemsSource="{Binding ListSpec}"
                      SeparatorVisibility="Default"
                      HasUnevenRows="True"
                      SelectedItem="{Binding SelectedSpec,Mode=TwoWay}"
                      ItemSelected="ioDocSpec_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout Orientation="Horizontal" >
                                    <Label Text="{Binding ModifName}"/>
                                    <Label Text="{Binding QuantFact}"/>
                                    <Label Text="{Binding Quant}"/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>